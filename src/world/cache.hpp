#pragma once

namespace landor::world
{

// Placeholder only. DESIGN_STATE.md D-19/D-22 make the cache a SectorCache
// owned by World; this skeleton exists so the header set compiles as a unit
// and is replaced by that interface.
class Cache
{
public:
    Cache();
};

} // namespace landor::world
